# ENDPOINTS-g3.md

Per-route authorization matrix for `project_documents`, `company_media`, `dashboard`, `dashboard_api`.

**Files read in full:** `.audit/FOUNDATION-A1.md`, `.audit/FOUNDATION-A2.md`, `.audit/FOUNDATION-B.md`, `app/project_documents/{__init__,routes,permissions,services}.py`, `app/company_media/{__init__,routes,permissions,services}.py`, `app/dashboard/{__init__,routes,services}.py`, `app/bulk_downloads/services.py`, `app/storage/services.py`, `app/auth/permissions.py`, `app/project_memberships.py`, `app/permissions/registry.py`, `app/models/{project_document,company_media,user}.py`, `app/audit.py`, `app/__init__.py:85-135`.
**Read in part:** `app/media_processing/services.py:90-112`, `app/templates/dashboard/contractor.html:48`, `.venv/.../flask_sqlalchemy/extension.py:739-765`, `.venv/.../sqlalchemy/orm/session.py:3764-3860`.

**Module-gate sanity check (matches FOUNDATION-A1 §2):**
- `project_documents` — own `@bp.before_request` at `app/project_documents/routes.py:25-27` → `can_access_project_documents()` (`permissions.py:21-23`) → `can_access_project_documents_module()` (`app/auth/permissions.py:73-78`). **Confirmed present.**
- `company_media` — own `@bp.before_request` at `app/company_media/routes.py:18-20` → `p.access()` (`permissions.py:36-41`). **Confirmed present.** Note this gate includes `has_album_acl(user)` (`permissions.py:18-33`), i.e. a bare album ACL row grants module entry; the project_documents gate has **no** equivalent folder-ACL clause.
- `dashboard` / `dashboard_api` — global `require_reports_module_access` (`app/__init__.py:176-189`; both prefixes present in the tuple at `:179`). **No `@bp.before_request` exists in `app/dashboard/routes.py`** (full read confirms).
- Blueprint prefixes: `/project-documents`, `/company-media`, `/reports/dashboard`, `/api/reports/dashboard`.
- **Rate limiting: `grep -rn "limiter" app/project_documents/ app/company_media/ app/dashboard/` returns zero matches.** Every row below is `NO`.

Abbreviations used in cells: `PD-perm` = `app/project_documents/permissions.py`, `PD-svc` = `app/project_documents/services.py`, `CM-perm` = `app/company_media/permissions.py`, `CM-svc` = `app/company_media/services.py`, `BD-svc` = `app/bulk_downloads/services.py`, `ST-svc` = `app/storage/services.py`, `DB-svc` = `app/dashboard/services.py`.

## Table — `project_documents` (23 handlers, 24 URL rules)

| Method | Path | Handler (file:line) | Module gate | RBAC check (file:line) | Per-project capability check (file:line) | Owns-resource check (file:line) | Input validation | Rate limited | Writes? | Issues presigned URL? | Dispatches Celery? | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GET | `/project-documents` and `/project-documents/` | `routes.py:32` (rules `:30`,`:31`) | Own `@bp.before_request` `routes.py:25-27` | Indirect: `can_create_custom_root` `PD-perm:17-18` (`project_documents.custom_roots.create`); `_base` `PD-perm:11-13` for each custom root | `list_accessible_projects` `PD-svc:43-50` → `accessible_project_ids(user,("can_view_documents",))` `project_memberships.py:103-111`; per-root `can_view_project_document_folder` `routes.py:33` (ACL-based via `_acl_allows` `PD-perm:35-43`) | N/A (no ID) | N/A | NO | NO | NO | NO | Loads **all** `project_id=None` roots then filters in Python (`routes.py:33`); each filter call lazy-loads `anchor.permissions` → N+1. |
| POST | `/project-documents/custom-roots` | `routes.py:37` | Own `@bp.before_request` | `can_create_custom_root` `routes.py:38` → `PD-perm:17-18`: `is_project_admin` OR `user.can("project_documents.custom_roots.create")` | NONE FOUND (custom roots have `project_id=None` by construction, `PD-svc:37`) | N/A (no ID) | `create_custom_root_folder` `PD-svc:31-40`: name required, dup-name check; `is_restricted` from `== "1"` | NO | YES (`ProjectDocumentFolder` + `AuditLog` `PD-svc:39`) | NO | NO | Creator receives **no** ACL row. If `is_restricted=1`, `_acl_allows` `PD-perm:38-43` finds an anchor with zero matching entries → the non-admin creator is locked out of the folder they just made. |
| GET | `/project-documents/projects/<int:project_id>` | `routes.py:47` | Own `@bp.before_request` | Indirect via `_base` `PD-perm:14` | `project not in list_accessible_projects(current_user)` `routes.py:49` (`can_view_documents`); then `can_view_project_document_folder(root)` `routes.py:51` | YES (`routes.py:48-49`: `Project.query.filter_by(id=..., deleted_at=None).first_or_404()` then membership test) | `<int:project_id>` converter | NO | **YES** — `get_or_create_project_root_folder` `PD-svc:19-28` INSERTs a folder + `AuditLog` + `commit()` on a **GET** | NO | NO | A read-capability-gated **GET performs a write**, so it is outside CSRF protection (CSRFProtect only covers non-GET). |
| GET | `/project-documents/folders/<int:folder_id>` | `routes.py:74` | Own `@bp.before_request` | Indirect via `_base` `PD-perm:11-13` (project-less roots) | `can_view_project_document_folder(..., include_archived=True)` `routes.py:76` → `_can` `PD-perm:46-48` = `_base` (`ProjectUser` flag `can_view_documents`, `PD-perm:14`) AND `_acl_allows` (**ACL-based**, `PD-perm:35-43`) | YES (`routes.py:75-76`: object loaded then authorized before use) | `folder_status`/`file_status` allow-listed `routes.py:57-62`; `q` unbounded string → `ilike` (parameterised) | NO | NO | NO | NO | Builds up to 8 permission dicts per file/child (`routes.py:81-105`); each `_can` walks `_restriction_anchor` (`PD-perm:26-32`) up the lazy `folder.parent` chain and reads `anchor.permissions`. Unbounded file/child count, no pagination → N+1 + O(depth × items). |
| POST | `/project-documents/folders/<int:folder_id>/files/presign-batch` | `routes.py:114` | Own `@bp.before_request` | Indirect: `project_document_files.upload` via `_base` `PD-perm:7,13` | `can_upload_project_document_folder` `routes.py:116` → `_can(...,"can_upload_documents","upload")` `PD-perm:57`; re-checked `PD-svc:78` | YES (`routes.py:115-116`); `selection_session_id` bound to actor+scope in `_selection_session` `ST-svc:89-97` | `create_upload_batch_presign` `ST-svc:32-54`: scope allow-list `ST-svc:14,34`, batch count/size caps, unique non-empty `client_file_id`; per-file `validate_file_metadata` (declared ext/MIME/size only — FOUNDATION-B §3) | NO | YES (`UploadBatch`, `UploadBatchItem`, `StorageObject`, `ST-svc:86`) | **YES** — `provider.create_presigned_upload` `ST-svc:75`. Immediately-preceding checks: `_require_active_user` `ST-svc:33`, scope allow-list `ST-svc:34`, `_check_phase_one_scope` `ST-svc:36` (**a literal no-op, `ST-svc:190-192`**). Folder authorization comes only from `routes.py:116` + `PD-svc:78`. | NO | POST-policy binds exact size (FOUNDATION-B §2). `ensure_storage_capacity` `ST-svc:50` is the check-then-act race from FOUNDATION-B §4. |
| POST | `/project-documents/folders/<int:folder_id>/files/upload-selection-sessions` | `routes.py:124` | Own `@bp.before_request` | Indirect (`can_upload_documents`) | `can_upload_project_document_folder` `routes.py:126` | YES (`routes.py:125-126`) | `create_upload_selection_session` `ST-svc:17-25`: scope allow-list, int coercion, 500-file / 2 GB caps | NO | YES (`UploadSelectionSession`, `ST-svc:25`) | NO | NO | Catches only `StorageValidationError` (`routes.py:129`); `_require_active_user` (`ST-svc:18`) raises `StorageAuthorizationError`, which is **not** caught → 500. Sibling `finalize_selection_session` does catch it (`routes.py:136`). |
| POST | `/project-documents/folders/<int:folder_id>/files/upload-selection-sessions/<int:session_id>/finalize` | `routes.py:132` | Own `@bp.before_request` | Indirect (`can_upload_documents`) | `can_upload_project_document_folder` `routes.py:134` | YES for both IDs — folder at `routes.py:133-134`; session at `ST-svc:93` (`session.created_by_id != user.id or module_type/target_type/target_id mismatch` → `StorageAuthorizationError`) | Status/expiry check `ST-svc:94-96` | NO | YES (session status → `completed`, `ST-svc:29`) | NO | NO | Session IDOR fully closed. |
| POST | `/project-documents/folders/<int:folder_id>/files/complete-upload` | `routes.py:141` | Own `@bp.before_request` | Indirect (`can_upload_documents`) | `can_upload_project_document_folder` `routes.py:143`; re-checked `PD-svc:133` | YES — folder `routes.py:142-143`; batch item bound to folder at `PD-svc:136` (`module_type != "project_documents" or target_type != "folder" or target_id != folder.id`); batch owner check `ST-svc:105,196-197` | `int(payload.get("upload_batch_item_id"))` in try/except → 400 `routes.py:145-146`; `_validate_head` size/MIME/checksum `ST-svc:159-168` | NO | YES (`StorageObject.upload_status="active"`, `ProjectDocumentFile`, `AuditLog`) | NO | **YES** (indirect) — `enqueue_media_processing_for_storage_object` `PD-svc:143`, double-wrapped in try/except (`PD-svc:141-146` and `media_processing/services.py:107-111`) | `create_project_document_file_from_storage_object` `PD-svc:116` checks **both** `upload_status != "active"` and `deleted_at is not None` — satisfies FOUNDATION-B §5's two-flag requirement. `PD-svc:118` (`folder.project_id != getattr(folder,"project_id",None)`) is a tautology and can never fire. |
| POST | `/project-documents/files/<int:file_id>/signed-download` | `routes.py:153` | Own `@bp.before_request` | Indirect: `can_view_documents` → `project_document_folders.view` `PD-perm:7,13`. **`project_document_files.download` is never consulted anywhere.** | `can_download_project_document_file` `routes.py:155` → `PD-perm:62-63`: `file.is_active and file.deleted_at is None and _can(file.folder, "can_view_documents","view")` (ACL-based via `_acl_allows`) | YES — file→folder→project chain re-derived from the DB (`PD-perm:63` uses `file.folder`, never a client-supplied `folder_id`) | 300 MB single-file cap `PD-svc:164-165`; `ensure_bandwidth` `PD-svc:167` | NO | YES (`DownloadEvent` + `AuditLog` `PD-svc:172-176`) | **YES** — `provider.create_presigned_download(...,"attachment")` `PD-svc:171`. Immediately preceding: `can_download_project_document_file` re-check `PD-svc:158`, then `upload_status != "active" or deleted_at is not None` **both-flag** check `PD-svc:161`. | NO | Permission checked twice (route + service). Good. |
| POST | `/project-documents/files/<int:file_id>/signed-preview` | `routes.py:161` | Own `@bp.before_request` | Indirect (`project_document_folders.view`) | `can_view_project_document_file` `routes.py:163` → `PD-perm:59-60` (checks `file.is_active`/`deleted_at`, then folder `_can`) | YES (file→folder chain, `PD-perm:60`) | `variant` mapped through fixed dicts `PD-svc:187-215`; unknown variant → `derivative_types=None` → 400 | NO | YES (`DownloadEvent` `PD-svc:200/212/241`) | **YES** — three branches: derivative URL `PD-svc:239`; **original object, inline** for `variant=="stream"` on `video/mp4|webm` `PD-svc:198`; **original object, inline** for `application/pdf` `PD-svc:210`. Immediately preceding each: `can_view_project_document_file` re-check `PD-svc:182` + both-flag `upload_status`/`deleted_at` check `PD-svc:194,206`. | NO | The two original-object branches are a deliberate exception to CLAUDE.md's "previews must not fall back to the original"; here the download authority is the *same* capability (`can_view_documents`), so no privilege is crossed. Contrast the company_media equivalent below, where it does cross one. |
| POST | `/project-documents/files/<int:file_id>/rename` | `routes.py:170` | Own `@bp.before_request` | Indirect: `project_document_folders.edit` `PD-perm:8,13` | `can_edit_project_document_file` `routes.py:172` → `PD-perm:65-66`; re-checked `PD-svc:247` | YES (file→folder, `PD-perm:66`) | `_display_name` `PD-svc:83-87` (non-empty, ≤255, no `/` `\`), extension lock `PD-svc:90-99`, in-folder dup check `PD-svc:102-112` | NO | YES (`display_name`, `AuditLog` `PD-svc:251`) | NO | NO | Redirect target uses `document_file.folder_id` from the DB, not client input. |
| POST | `/project-documents/files/<int:file_id>/archive` | `routes.py:179` | Own `@bp.before_request` | Indirect: `project_document_files.delete` `PD-perm:8,13` | `can_delete_project_document_file` `routes.py:181` → `PD-perm:68-69`; re-checked `PD-svc:256` | YES (file→folder) | none needed | NO | YES (`is_active=False`, `deleted_at`, `AuditLog` `PD-svc:258`) | NO | NO | No `provider.delete_object`; storage object and derivatives are retained (soft delete). |
| POST | `/project-documents/files/<int:file_id>/restore` | `routes.py:187` | Own `@bp.before_request` | Indirect: `project_document_files.restore` `PD-perm:9,13` | `can_restore_project_document_file` `routes.py:189` → `PD-perm:71-72` (`include_archived=True`, ACL action `"delete"`); re-checked `PD-svc:263` | YES (file→folder) | parent folder must be active `PD-svc:264` | NO | YES (`AuditLog` `PD-svc:266`) | NO | NO | ACL flag used for *restore* is `can_delete` (action string `"delete"`, `PD-perm:72`) — no `can_restore` column exists. |
| POST | `/project-documents/folders/<int:folder_id>/files/bulk-archive` | `routes.py:214` | Own `@bp.before_request` | Indirect (`project_document_files.delete`, per file) | Route gate is **view only**: `_bulk_folder_or_403` `routes.py:199-203` → `can_view_project_document_folder`. Delete authority proven **per file** at `PD-svc:299` | YES, per-ID — `_bulk_files_in_folder` `PD-svc:285-289` filters `folder_id == folder.id` AND `id.in_(...)`, then `can_delete_project_document_file` per file `PD-svc:299` | `_normalize_file_ids` `PD-svc:269-282`: int-coerces, dedups, **caps at 50**, rejects empty; `parse_file_ids` `BD-svc:21-41` accepts JSON `file_ids`, form `file_ids_json`, or `file_ids[]` | NO | YES (per-file `AuditLog` `PD-svc:305`) | NO | NO | `_bulk_response` `routes.py:206-210`: 403 only when `archived==0 and forbidden>0`; a partial batch returns 200 with a `forbidden` count. |
| POST | `/project-documents/folders/<int:folder_id>/files/bulk-restore` | `routes.py:223` | Own `@bp.before_request` | Indirect (`project_document_files.restore`, per file) | Route gate view-only (`routes.py:224`); restore authority per file `PD-svc:318` | YES, per-ID (`PD-svc:285-289` + `PD-svc:318`) | same as above; folder-active check `PD-svc:320` | NO | YES (per-file `AuditLog` `PD-svc:326`) | NO | NO | `_bulk_folder_or_403` uses `include_archived=False`, so bulk-restore inside an archived folder 403s at the route before the service's "skipped" branch is reached. |
| POST | `/project-documents/folders/<int:folder_id>/files/bulk-signed-download` | `routes.py:232` | Own `@bp.before_request` | Indirect (`project_document_folders.view`, per file) | Route gate view-only (`routes.py:233`); download authority per file at `BD-svc:224` | YES, per-ID — `_document_files` `BD-svc:211-213` filters `folder_id == folder.id`; `_validate_selected` `BD-svc:199-208` raises if `len(files) != len(ids)` (cross-folder ID → hard error, not silent drop) and re-checks `is_active`/`deleted_at`/`can_download_project_document_file` **per file** | `_normal_ids` `BD-svc:264-267` int-coerces; ≤100 files `BD-svc:203`; ≤300 MB total `BD-svc:205`; `_ensure_objects_exist` head-checks every object `BD-svc:235-242` | NO | YES (`DownloadEvent` `BD-svc:102`; plus `AuditLog` on the 1-file path) | **YES for the 1-file "direct" case** — `create_file_download_url` `BD-svc:60` → presigned URL at `PD-svc:171`, preceded by the per-file `can_download_project_document_file` re-check `PD-svc:158`. Multi-file returns a server-built ZIP body, no presigned URL to the client. | NO | Satisfies FOUNDATION-B §7. ZIP is built **synchronously in the web worker** (`BD-svc:87-93`), holding a Gunicorn thread for the whole archive; bounded by 100 files / 300 MB. |
| POST | `/project-documents/folders/<int:folder_id>/files/bulk-download-validate` | `routes.py:244` | Own `@bp.before_request` | Indirect (per file) | Route gate view-only (`routes.py:245`); per-file `can_download_project_document_file` `BD-svc:224` | YES, per-ID (`BD-svc:211-213` + `BD-svc:199-208`) | same caps as above | NO | NO | NO | NO | Returns only `{"kind": "direct"|"zip"}`. `_ensure_objects_exist` performs one `head_object` per selected file — unauthenticated-to-storage cost amplification is bounded by the 100-file cap. |
| GET | `/project-documents/bulk-download-jobs/<int:job_id>` | `routes.py:255` | Own `@bp.before_request` | NONE FOUND (no permission code); role fallback inside `_check_job_access` `BD-svc:281` (`SUPER_ADMIN`/`ADMIN` string literals, not `user.can`) | NONE FOUND | **Partial** — `_check_job_access` `BD-svc:280-282` requires `user.id == job.requested_by_id` or ADMIN/SUPER_ADMIN. **No check that `job.module == "document-library"`**, so a `company_media` job is serviceable through this URL (still requester-bound). | `<int:job_id>` converter | NO | NO | **YES if `job.status=="succeeded"`** — `create_presigned_download` `BD-svc:123`, preceded by `_check_job_access` `BD-svc:119` and `job.expires_at > _now()` `BD-svc:122`. **No re-validation that the requester still has access to the underlying files.** | NO | Per FOUNDATION-B §6, `_create_job` (`BD-svc:188`) has **zero callers**, so no new `BulkDownloadJob` rows are produced by any reachable route → dead path, Info-level. |
| POST | `/project-documents/folders/new` | `routes.py:269` | Own `@bp.before_request` | Indirect: `project_document_folders.edit` `PD-perm:8,13`. (`project_document_folders.create` exists in the registry but is **never consulted**.) | `can_create_project_document_folder(parent)` `routes.py:271` → `PD-perm:52` (reuses `can_edit_documents`/action `"edit"`) | YES for `parent_id` (`routes.py:270-271`) — **the only route in this blueprint whose primary object ID arrives in the request body** | `_name` `PD-svc:360-364` (non-empty, ≤255, no `/` `\` `.` `..`); sibling-name dup check `PD-svc:380`; parent must be active `PD-svc:378` | NO | YES (`ProjectDocumentFolder`, `AuditLog` `PD-svc:384`) | NO | NO | Missing `parent_id` → `db.get_or_404(ProjectDocumentFolder, None)`. Verified experimentally against the installed SQLAlchemy 2.0.51: `session.get(Model, None)` returns `None` (emitting `SAWarning: fully NULL primary key identity cannot load any object`) → a clean 404 in production, but a **hard test failure** under `pytest.ini`'s `filterwarnings = error`. |
| POST | `/project-documents/folders/<int:folder_id>/rename` | `routes.py:278` | Own `@bp.before_request` | Indirect (`project_document_folders.edit`) | `can_edit_project_document_folder` `routes.py:280` → `PD-perm:53` | YES (`routes.py:279-280`) | root folders rejected `PD-svc:390`; `_name` + sibling dup `PD-svc:392` | NO | YES (`AuditLog` `PD-svc:394`) | NO | NO | — |
| POST | `/project-documents/folders/<int:folder_id>/move` | `routes.py:287` | Own `@bp.before_request` | Indirect (`project_document_folders.edit`) | **Both** objects checked: `can_edit_project_document_folder(target)` AND `can_create_project_document_folder(destination)` `routes.py:289` | YES for **both** IDs (`routes.py:288-289`); `move_folder` additionally enforces `folder.project_id == new_parent.project_id` `PD-svc:407` and rejects self/descendant `PD-svc:409` | root rejected `PD-svc:406`; destination must be active `PD-svc:408`; cycle prevention `_is_descendant` `PD-svc:397-402`; sibling dup `PD-svc:410` | NO | YES (`AuditLog` `PD-svc:412`) | NO | NO | Best-checked route in the blueprint — confused-deputy (permitted parent + unauthorized child) is explicitly closed here. Also closes A2's "no DB-level folder cycle prevention" gap in application code. |
| POST | `/project-documents/folders/<int:folder_id>/archive` | `routes.py:296` | Own `@bp.before_request` | Indirect: `project_document_files.delete` `PD-perm:8,13`. (`project_document_folders.delete` exists in the registry, **never consulted**.) | `can_delete_project_document_folder` `routes.py:298` → `PD-perm:54` | YES (`routes.py:297-298`) | root rejected `PD-svc:416` | NO | YES (`AuditLog` `PD-svc:418`) | NO | NO | `archive_folder` `PD-svc:415-418` does **not** cascade to children or files — a direct URL to a child folder/file of an archived parent still resolves and `_can` still passes, because `_restriction_anchor` walks parents but no ancestor-archived state is checked. Uses `Markup(f'...href="{archived_url}"...')` `routes.py:303`: `archived_url` comes from `url_for` with the user-supplied `q`, which Werkzeug percent-encodes, so the quote-break is not reachable — but this is the only autoescape bypass in the file. |
| POST | `/project-documents/folders/<int:folder_id>/restore` | `routes.py:308` | Own `@bp.before_request` | Indirect: `project_document_files.restore` `PD-perm:9,13`. (`project_document_folders.restore` **never consulted**.) | `can_restore_project_document_folder` `routes.py:310` → `PD-perm:55` (`include_archived=True`, ACL action `"delete"`) | YES (`routes.py:309-310`) | root rejected `PD-svc:422`; parent must be active `PD-svc:423`; sibling dup `PD-svc:424` | NO | YES (`AuditLog` `PD-svc:426`) | NO | NO | — |
| GET, POST | `/project-documents/folders/<int:folder_id>/permissions` | `routes.py:317` | Own `@bp.before_request` | Indirect: `project_document_folders.share` `PD-perm:9,13` | `can_share_project_document_folder(..., include_archived=not (target.is_active and target.deleted_at is None))` `routes.py:319` → `PD-perm:56` | YES for `folder_id` (`routes.py:318-319`); for `remove_id`, `remove_folder_permission` `PD-svc:463` filters `id=permission_id, folder_id=folder.id` | `set_folder_permission` `PD-svc:429-459`: `principal_type in {"user","role"}`, `isdigit()`, `>0`, principal must exist, user must be `is_active`, ≥1 of 5 flags | NO | YES (`ProjectDocumentFolderPermission` + `AuditLog` `document.folder.share` / `document.folder.revoke` `PD-svc:458,465`) | NO | NO | **No restriction on which flags the actor may grant** — but escalation is neutralised because `_can` `PD-perm:48` AND-s the ACL with `_base`, so an ACL `can_delete` grant is inert unless the grantee separately holds `can_archive_documents`. GET leaks the full active-user roster (`full_name`, `username`, `email`, role) `routes.py:327-337` to anyone with share rights on any folder. ACL rows written to a **non-restricted** folder are inert, because `_acl_allows` `PD-perm:38-40` returns `True` before consulting them and only the nearest `is_restricted` **ancestor** is ever read. |

## Table — `company_media` (23 handlers, 24 URL rules)

| Method | Path | Handler (file:line) | Module gate | RBAC check (file:line) | Per-project capability check (file:line) | Owns-resource check (file:line) | Input validation | Rate limited | Writes? | Issues presigned URL? | Dispatches Celery? | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GET | `/company-media/` and `/company-media` | `routes.py:23` (rules `:21`,`:22`) | Own `@bp.before_request` `routes.py:18-20` | `p.create_album` `routes.py:29` → `CM-perm:71`; per-album `view_album` → `_can` `CM-perm:55-68` (`company_media_albums.view`) | N/A — albums carry no `project_id`. Scope is the **album ACL** (`CompanyMediaAlbumPermission`, `_matching_acl_allows` `CM-perm:44-46`) | N/A (no ID) | `album_status` allow-listed `routes.py:24`; `q` unbounded → `ilike` (parameterised) | NO | NO | NO | NO | 2 cover queries per album (`routes.py:27-28`) + `album.permissions` lazy-load per `view_album` → N+1 on both. No pagination. |
| POST | `/company-media/albums/create` | `routes.py:31` | Own `@bp.before_request` | `p.create_album` `routes.py:32` → `CM-perm:71`: `has_module_access` AND role ≠ VIEWER_ADMIN AND `user.can("company_media_albums.create")` | N/A | N/A | `_name` `CM-svc:10-13` (non-empty, ≤255); dup-name check `CM-svc:30` | NO | YES (`CompanyMediaAlbum`, `AuditLog` `CM-svc:31`) | NO | NO | Unlike `can_create_custom_root`, this one **does** require module access — and an `ADMIN` still needs the RBAC grant (no `is_project_admin` bypass). |
| GET | `/company-media/albums/<int:album_id>` | `routes.py:37` | Own `@bp.before_request` | `p.view_album(...,True)` `routes.py:39` → `_can` `CM-perm:55-68` (`company_media_albums.view`) | ACL-based `_matching_acl_allows` `CM-perm:66` / `_acl` `CM-perm:49-52` | YES (`routes.py:38-39`) | `media_status` allow-listed `routes.py:17` | NO | NO | NO | NO | `archived=True` → archived albums are viewable, mirroring `project_documents.folder`'s `include_archived=True`. |
| POST | `/company-media/albums/<int:album_id>/rename` | `routes.py:43` | Own `@bp.before_request` | `p.edit_album` `routes.py:45` → `CM-perm:74` (`company_media_albums.edit`) | ACL-based (`can_edit`) `CM-perm:66` | YES (`routes.py:44-45`) | `_name` `CM-svc:32` | NO | YES (`AuditLog` `CM-svc:32`) | NO | NO | — |
| POST | `/company-media/albums/<int:album_id>/archive` | `routes.py:50` | Own `@bp.before_request` | `p.delete_album` `routes.py:52` → `CM-perm:75` (`company_media_albums.delete`) | ACL-based (`can_delete`) | YES (`routes.py:51-52`) | none | NO | YES (`AuditLog` `CM-svc:33`) | NO | NO | Does not cascade to member files (same shape as `archive_folder`). |
| POST | `/company-media/albums/<int:album_id>/restore` | `routes.py:55` | Own `@bp.before_request` | `p.restore_album` `routes.py:57` → `CM-perm:76` (`company_media_albums.restore`, `archived=True`, ACL action `"delete"`) | ACL-based (`can_delete`) | YES (`routes.py:56-57`) | none | NO | YES (`AuditLog` `CM-svc:34`) | NO | NO | — |
| POST | `/company-media/albums/<int:album_id>/cover` | `routes.py:60` | Own `@bp.before_request` | `p.edit_album` `routes.py:62` | ACL-based (`can_edit`) | YES for **both** IDs — album `routes.py:61-62`; `set_cover` `CM-svc:76` enforces `f.album_id != a.id or not f.is_active or f.deleted_at` → rejects | `media_id` via `type=int` `routes.py:63` | YES (`cover_media_id`, `AuditLog` `CM-svc:77`) | NO | NO | Cross-album cover injection closed here in addition to the `before_flush` guard (`app/models/company_media.py:62-68`). |
| POST | `/company-media/albums/<int:album_id>/cover/clear` | `routes.py:67` | Own `@bp.before_request` | `p.edit_album` `routes.py:69` | ACL-based (`can_edit`) | YES (`routes.py:68-69`) | N/A | NO | YES (`a.cover_media_id=None; commit()` `routes.py:70`) — **no `AuditLog`** | NO | NO | Sibling `cover` audits (`CM-svc:77`); this one does not. Field mutated inline in the route rather than in `services.py`. |
| POST | `/company-media/albums/<int:album_id>/files/presign-batch` | `routes.py:72` | Own `@bp.before_request` | `p.upload_album` `routes.py:74` → `CM-perm:73` (`company_media_files.upload`) | ACL-based (`can_upload`) | YES (`routes.py:73-74`); `selection_session_id` bound to actor+scope `ST-svc:93` | `create_upload_batch_presign` `ST-svc:32-54` (as for project_documents) | NO | YES (`UploadBatch`/`Item`/`StorageObject`) | **YES** — `provider.create_presigned_upload` `ST-svc:75`. Immediately preceding: `_require_active_user` `ST-svc:33`, scope allow-list `ST-svc:34`, no-op `_check_phase_one_scope` `ST-svc:36`. Album authorization comes **only** from `routes.py:74` — `s.presign` `CM-svc:35` performs **no** re-check (contrast `PD-svc:78`). | NO | `except Exception as e: return jsonify(error=str(e)),400` `routes.py:78` returns the **raw exception string** to the client — SQLAlchemy/boto3 internals can leak. Sibling `presign_batch` `routes.py:120` catches only the two typed validation errors. |
| POST | `/company-media/albums/<int:album_id>/files/upload-selection-sessions` | `routes.py:80` | Own `@bp.before_request` | `p.upload_album` `routes.py:82` | ACL-based (`can_upload`) | YES (`routes.py:81-82`) | `ST-svc:17-25` caps | NO | YES (`UploadSelectionSession`) | NO | NO | Same uncaught-`StorageAuthorizationError` gap as the project_documents sibling (`routes.py:85` catches only `StorageValidationError`). `request.get_json()` without `silent=True` `routes.py:83` → 415 on a non-JSON body. |
| POST | `/company-media/albums/<int:album_id>/files/upload-selection-sessions/<int:session_id>/finalize` | `routes.py:87` | Own `@bp.before_request` | `p.upload_album` `routes.py:89` | ACL-based (`can_upload`) | YES for both IDs — album `routes.py:88-89`; session `ST-svc:93` (owner + module + target_type + target_id) | `ST-svc:94-96` status/expiry | NO | YES (session → `completed`) | NO | NO | — |
| POST | `/company-media/albums/<int:album_id>/files/complete-upload` | `routes.py:94` | Own `@bp.before_request` | `p.upload_album` `routes.py:96` | ACL-based (`can_upload`) | **Partial** — album `routes.py:95-96`; batch item bound at `CM-svc:38` on `module_type` and `target_id` but **`target_type` is not checked** (sibling `PD-svc:136` does check it). Batch owner check `ST-svc:105,196-197` | `int(data.get("upload_batch_item_id"))` **not** wrapped → `TypeError`/`ValueError` → 500 (`routes.py:97`); sibling returns 400 (`routes.py:145-146`). No try/except around `s.complete` at all. `_validate_head` `ST-svc:159-168` | NO | YES (`StorageObject`→active, `CompanyMediaFile`, `AuditLog` `CM-svc:42`) | NO | **YES** — `enqueue_media_processing_for_storage_object` `CM-svc:44`, **not** wrapped locally (the callee swallows dispatch errors at `media_processing/services.py:107-111`, so no user-visible failure) | The missing `target_type` check is currently inert: `VALID_SCOPES` `ST-svc:14` admits only `("company_media","album")`, so any `company_media` batch necessarily has `target_type=="album"`. `CM-svc:40` raises *after* `complete_upload_item` already flipped `upload_status` to `active` and committed (`ST-svc:119-124`) — a non-image/video upload leaves an orphan active `StorageObject` with no `CompanyMediaFile`, counted against quota, with no cleanup path (`cleanup_pending_uploads` only sweeps `pending`, `ST-svc:143`). |
| POST | `/company-media/files/<int:file_id>/signed-preview` | `routes.py:99` | Own `@bp.before_request` | `p.view_file` `routes.py:101` → `CM-perm:78` (`company_media_files.view`) | ACL-based (`can_view`) via `_can(file.album, ...)` | **Partial** — album membership YES (`CM-perm:78` derives `file.album`, never trusts a client `album_id`), **but `view_file` `CM-perm:78` does not check `file.is_active` / `file.deleted_at`**, unlike `download_file`/`edit_file`/`delete_file` (`CM-perm:79-81`) and unlike `can_view_project_document_file` (`PD-perm:59-60`). An **archived/soft-deleted** file is previewable. | `variant` free string; only `{"preview","stream"}` and the derivative loop consume it | NO | YES (`DownloadEvent` `CM-svc:51,59`) | **YES** — derivative URL `CM-svc:60`, **or the ORIGINAL object inline** `CM-svc:52` when `mime_type in {"video/mp4","video/webm"} and variant in {"preview","stream"}`. Immediately preceding check: `p.view_file` at `routes.py:101` only — `s.signed_preview` performs **no** re-check (contrast `PD-svc:182`). | NO | Two problems compound here: (a) the original-object branch is reachable with `variant="preview"`, so a caller holding only `company_media_files.view` / ACL `can_view` obtains the full original video bytes, **bypassing `company_media_files.download` and the ACL `can_download` flag** — the sibling project_documents branch requires `variant=="stream"` (`PD-svc:192`) and its download authority is the same capability, so no privilege is crossed there; (b) combined with the missing file-state check, archived media is retrievable. `StorageDerivative` lookup `CM-svc:54` filters `deleted_at` but **never checks the parent `StorageObject.upload_status`/`deleted_at`** — the exact two-flag gap FOUNDATION-B §5 left open for this module (sibling `PD-svc:194,206` does check both, but only on its original-object branches). |
| POST | `/company-media/files/<int:file_id>/signed-download` | `routes.py:104` | Own `@bp.before_request` | `p.download_file` `routes.py:106` → `CM-perm:79` (`company_media_files.download`) | ACL-based (`can_download`) | YES — `CM-perm:79` checks `file.is_active`, `not file.deleted_at`, and `_can(file.album,...)` | 300 MB cap `CM-svc:68`; `ensure_bandwidth` `CM-svc:70` | NO | YES (`DownloadEvent` `CM-svc:72`) — **no `AuditLog`** (sibling `PD-svc:172` writes `document.file.download`) | **YES** — `create_presigned_download(...,"attachment")` `CM-svc:73`. Immediately preceding: `p.download_file` at `routes.py:106` only; `s.signed_download` `CM-svc:64-73` performs **no** permission re-check and **no** `upload_status`/`deleted_at` check on the `StorageObject`. | NO | Route passes `current_user` explicitly, so the `user = user or db.session.get(User, f.created_by_id)` fallback at `CM-svc:67` is not reached here. |
| POST | `/company-media/files/<int:file_id>/rename` | `routes.py:110` | Own `@bp.before_request` | `p.edit_file` `routes.py:112` → `CM-perm:80` (`company_media_files.edit`) | ACL-based (`can_edit`) | YES (`CM-perm:80` checks file state + album) | **Only** `strip()` + non-empty `routes.py:113-114`. **No ≤255 length check, no `/`/`\` rejection, no extension lock, no in-album duplicate check** — sibling `rename_file` (`PD-svc:245-251`) does all four | NO | YES (`display_name`, `updated_by_id`, `commit()` `routes.py:115`) — **no `AuditLog`** | NO | NO | `display_name` is `String(255)`; a longer value raises a PostgreSQL `DataError` at commit → unhandled 500. Field mutated inline in the route, not via `services.py`. |
| POST | `/company-media/files/<int:file_id>/archive` | `routes.py:118` | Own `@bp.before_request` | `p.delete_file` `routes.py:120` → `CM-perm:81` (`company_media_files.delete`) | ACL-based (`can_delete`) | YES (`CM-perm:81`) | none | NO | YES (`is_active=False`, `deleted_at`, `commit()` `routes.py:121`) — **no `AuditLog`** | NO | NO | Sibling `archive_file` `PD-svc:258` audits `document.file.archive`. Uses `__import__('datetime').datetime.utcnow()` inline. |
| POST | `/company-media/files/<int:file_id>/restore` | `routes.py:123` | Own `@bp.before_request` | `p.restore_file` `routes.py:125` → `CM-perm:82` (`company_media_files.restore`, `archived=True`, ACL action `"delete"`) | ACL-based (`can_delete`) | YES (`CM-perm:82` + `f.album.is_active` `routes.py:125`) | none | NO | YES (`commit()` `routes.py:126`) — **no `AuditLog`** | NO | NO | Sibling `restore_file` `PD-svc:266` audits. |
| POST | `/company-media/albums/<int:album_id>/files/bulk-archive` | `routes.py:141` → `_bulk` `routes.py:127` | Own `@bp.before_request` | Per-file `p.delete_file` `routes.py:134` (`company_media_files.delete`) | Route gate is **view only**: `p.view_album` `routes.py:129`; delete authority per file `routes.py:134` | YES, per-ID — `routes.py:132` filters `album_id == a.id` AND `id.in_(ids)`, then per-file `p.delete_file` `routes.py:134` | `parse_file_ids` `BD-svc:21-41` only. **No int coercion, no dedup, no count cap** — sibling `_normalize_file_ids` `PD-svc:269-282` caps at 50 and coerces | NO | YES (`is_active=False`, `deleted_at`, `commit()` `routes.py:136,139`) — **no `AuditLog`** (sibling `bulk_archive_files` audits per file, `PD-svc:305`) | NO | NO | Raw list flows into `CompanyMediaFile.id.in_(ids)` `routes.py:132`: a non-numeric element yields a PostgreSQL `invalid input syntax for type bigint` → 500; an arbitrarily long list yields an arbitrarily large `IN` clause. Unlike `_bulk_response` `routes.py:206-210`, `_bulk` returns 200 even when `forbidden==len(items)` and nothing changed. |
| POST | `/company-media/albums/<int:album_id>/files/bulk-restore` | `routes.py:143` → `_bulk` `routes.py:127` | Own `@bp.before_request` | Per-file `p.restore_file` `routes.py:134` (`company_media_files.restore`) | Route gate view-only `routes.py:129`; restore authority per file + `a.is_active` `routes.py:134` | YES, per-ID (`routes.py:132,134`) | same as bulk-archive (none) | NO | YES (`commit()` `routes.py:137,139`) — **no `AuditLog`** | NO | NO | Same untyped/uncapped ID-list issue. |
| POST | `/company-media/albums/<int:album_id>/files/bulk-signed-download` | `routes.py:145` | Own `@bp.before_request` | Per-file `download_file` `BD-svc:231` (`company_media_files.download`) | Route gate view-only `routes.py:147`; download authority per file `BD-svc:231` | YES, per-ID — `_media_files` `BD-svc:216-218` filters `album_id == album.id`; `_validate_selected` `BD-svc:199-208` rejects when `len(files) != len(ids)` and re-checks `is_active`/`deleted_at`/`download_file` per file | `_normal_ids` int-coerces `BD-svc:264-267`; ≤100 files, ≤300 MB `BD-svc:203-206`; `_ensure_objects_exist` `BD-svc:235` | NO | YES (`DownloadEvent` `BD-svc:102` / `CM-svc:72`) | **YES for the 1-file "direct" case** — `signed_download(files[0], user)` `BD-svc:68` → `CM-svc:73`, preceded by the per-file `download_file` re-check `BD-svc:231`. Multi-file returns a server-built ZIP body. | NO | Correctly passes `user` into `signed_download`, so bandwidth/`DownloadEvent` attribution is right on this path (contrast `_bulk`'s `"downloads"` branch below). Synchronous ZIP in the web worker. |
| POST | `/company-media/albums/<int:album_id>/files/bulk-download-validate` | `routes.py:157` | Own `@bp.before_request` | Per-file `download_file` `BD-svc:231` | Route gate view-only `routes.py:159`; per-file `BD-svc:231` | YES, per-ID (`BD-svc:216-218` + `BD-svc:199-208`) | same caps | NO | NO | NO | NO | — |
| GET | `/company-media/bulk-download-jobs/<int:job_id>` | `routes.py:168` | Own `@bp.before_request` | NONE FOUND; role fallback in `_check_job_access` `BD-svc:281` (literal `"SUPER_ADMIN"`/`"ADMIN"`) | NONE FOUND | **Partial** — requester-or-admin `BD-svc:280-282`; **`job.module` not verified** against the blueprint | `<int:job_id>` | NO | NO | **YES if succeeded** — `create_presigned_download` `BD-svc:123`, preceded by `_check_job_access` `BD-svc:119` and the expiry test `BD-svc:122`; no re-validation of file-level access | NO | Byte-identical logic to the project_documents sibling; both are dead per FOUNDATION-B §6 (`_create_job` has no callers). |
| GET, POST | `/company-media/albums/<int:album_id>/permissions` | `routes.py:173` | Own `@bp.before_request` | `p.share_album(...,True)` `routes.py:175` → `CM-perm:77` (`company_media_albums.share`) | ACL-based (`can_share`) — and per `CM-perm:66-67` an ACL `can_share` grant **alone** satisfies this, with no module access and no RBAC code | YES for `album_id` (`routes.py:174-175`); for `remove_id`, `routes.py:178` filters `id=..., album_id=a.id` | `set_permission` `CM-svc:78-95`: `typ in {"user","role"}`, `isdigit()`, `>0`, principal must exist, user must be `is_active`, ≥1 of **6** flags | NO | YES (`CompanyMediaAlbumPermission` `CM-svc:95`; delete `routes.py:180`) — **no `AuditLog` on either grant or revoke** (sibling audits both, `PD-svc:458,465`) | NO | NO | **Privilege escalation.** `set_permission` places no bound on which flags the actor may grant, and unlike project_documents there is **no second `_base` gate** to neutralise it (`_can` `CM-perm:66-67` returns `True` on a bare ACL match). A user whose only authority is an ACL `can_share` row can insert an entry naming **themselves** with `can_edit`/`can_delete`/`can_upload`/`can_download=1`, or name a **role** to grant the whole role, and thereby gain album/file delete and upload rights they were never granted. GET leaks the full active-user roster (`full_name`, `username`, `email`, role) `routes.py:185-195`. |

## Table — `dashboard` + `dashboard_api` (7 handlers)

| Method | Path | Handler (file:line) | Module gate | RBAC check (file:line) | Per-project capability check (file:line) | Owns-resource check (file:line) | Input validation | Rate limited | Writes? | Issues presigned URL? | Dispatches Celery? | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GET | `/reports/dashboard/system` | `routes.py:24` | Global hook `app/__init__.py:176-189` (`"dashboard."` at `:179`) → `can_access_reports_module` `auth/permissions.py:51-56` | `_require_dashboard_scope("dashboards.system.view")` `routes.py:25` → `routes.py:124-130`: reports module AND `user.can("dashboards.system.view")` AND `user.can("projects.scope_all")` | **Data-level filter, cited:** `DashboardScope.projects_query()` `DB-svc:91-103` — `Project.deleted_at.is_(None)` (`:92`), then `project_ids = accessible_project_ids(current_user, ("can_view_project",))` (`:100`) and `query.filter(Project.id.in_(project_ids or [0]))` (`:102`) whenever that returns non-`None`. Every downstream aggregate consumes only `active_project_ids`/`all_project_ids` derived from it (`DB-svc:219-221` → `:248-255,261-265`, `_section_status_counts` `:550`, `_section_status_trend` `:569`, `_issue_dashboard_counts` `:594`, `_active_contractor_counts` `:618`, `_recent_project_updates` `:637`, `_recent_daily_reports` `:652`). **The query is scoped, not filtered-at-display.** | N/A (no ID) | none accepted | NO | NO | NO | NO | Because the route already demands `projects.scope_all`, `accessible_project_ids` returns `None` (`project_memberships.py:104`) for every caller who reaches it → the `in_()` filter is not applied, which is coherent rather than a bypass. `dashboard_navigation_context` `routes.py:133-173` also scopes its customer/project/contractor lists to `visible_project_ids` (`routes.py:139,142,146,153`). Perf: loads **all** in-scope projects and all customers/contractors with no pagination; `_system_dashboard_analytics` `DB-svc:659-704` builds a 7-branch `UNION ALL` with `Project.id IN (<every project id>)`. |
| GET | `/reports/dashboard/customers/<int:customer_id>` | `routes.py:37` | Global hook | `_require_dashboard_scope("dashboards.customer.view")` `routes.py:38` → `routes.py:124-130` (also requires `projects.scope_all`) | Same `projects_query()` scoping `DB-svc:91-103`, additionally narrowed by `Project.customer_id == self.customer_id` `DB-svc:93-94` | YES (ordering is deliberate) — RBAC at `routes.py:38` runs **before** `Customer.query.filter_by(id=customer_id, is_active=True).first_or_404()` `routes.py:41`; comment at `routes.py:39-40` states this is to prevent Customer-ID enumeration. Verified correct. | `<int:customer_id>`; `is_active=True` filter | NO | NO | NO | NO | A customer with zero in-scope projects still renders (empty) and discloses `customer.name` — not a leak, since every caller holds `projects.scope_all`. |
| GET | `/reports/dashboard/contractors/<int:contractor_id>` | `routes.py:54` | Global hook | `_require_contractor_dashboard()` `routes.py:55` → `routes.py:119-121`: reports module AND `user.can("dashboards.contractor.view")`. **Does not require `projects.scope_all`** (unlike the two routes above) | **Data-level filter, cited:** `contractor_projects_query` `DB-svc:379-386` = `DashboardScope.contractor(id).projects_query()` (scoped by `accessible_project_ids`, `DB-svc:100-102`) `.join(ProjectContractorAssignment).filter(ProjectContractorAssignment.contractor_id == contractor_id)`; `_contractor_assignments_query` `DB-svc:410-411` then applies `ProjectContractorAssignment.project_id.in_(visible_projects)`. Downstream `updates`/`latest_reports`/`issues` all key off `assignment_ids`/`project_ids` subqueries from that query (`DB-svc:461-462,465,482,494`). | YES — `_contractor_or_404` `routes.py:112-116`: loads by ID then `contractor_is_visible(contractor.id)` `DB-svc:389-395`, which for non-admins requires an assignment to a project the caller can view. **Does not filter `is_active`**, so an archived contractor is viewable (sibling `customer_dashboard` does filter `is_active=True`, `routes.py:41`). | `parse_contractor_dashboard_filters` `DB-svc:366-376`: `assignment_status` allow-listed against `CONTRACTOR_ASSIGNMENT_STATUSES ∪ {"ALL"}` → 400; `project_id` via `type=int`, then re-verified to belong to the contractor's visible set `DB-svc:412-415` → `DashboardFilterError` → 404 | NO | NO | NO | NO | **Cross-project issue leakage.** `can_view_issues = has_any_project_capability(current_user, ("can_view_issues",))` `DB-svc:490-491` is a **global any-project boolean**, while the `issues` query `DB-svc:492-497` is scoped only by `project_ids` (derived from `can_view_project`). A member holding `can_view_issues` on project A and merely `can_view_project` on project B sees project B's issues here — and `app/templates/dashboard/contractor.html:48` renders `issue.title` and `issue.project.code`. The sibling `project_dashboard_context` uses the correct **per-project** `user_has_project_capability(current_user, project.id, "can_view_issues")` at `DB-svc:128-129`. |
| GET | `/api/reports/dashboard/projects/<int:project_id>/section-status` | `routes.py:67` | Global hook (`"dashboard_api."` at `app/__init__.py:179`) | Inline `routes.py:68`: `can_access_reports_module(current_user)` AND `current_user.can("dashboards.project.view")`. **Does not require `projects.scope_all`** | `can_read_project(project.id)` `routes.py:71` → `auth/permissions.py:146-147` → `user_has_project_capability(user, project_id, "can_view_project")` `project_memberships.py:78-89`. All downstream queries are hard-filtered to `DailyReport.project_id == project.id` / `PersistentIssue.project_id == project.id` (`DB-svc:172,177,182,186,189,191`) | YES (`routes.py:70-71`: `filter_by(id=project_id, deleted_at=None).first_or_404()` then capability check before use) | `selected_date` parsed with `datetime.strptime(raw_date,"%Y-%m-%d")` in try/except → `abort(400)` `routes.py:73-77`. `days` is not client-controllable (fixed at 7, `DB-svc:166`) | NO | NO | NO | NO | Response includes `persistent_issues.total` and `by_status` (`DB-svc:206`, from `DB-svc:185-187`) with **no `can_view_issues` check at all** — a `CUSTOM` membership holding only `can_view_project` gets the issue count breakdown. Aggregate-only (no titles), but it diverges from `project_dashboard_context`'s gating at `DB-svc:129,136`. Also returns `latest_project_update.title` (`DB-svc:207`), ungated (no project-update capability exists in `CAPABILITY_FIELDS`). |
| GET | `/api/reports/dashboard/system/overview` | `routes.py:82` | Global hook | `_require_dashboard_scope("dashboards.system.view")` `routes.py:83` (identical to the HTML route) | Same `projects_query()` scoping `DB-svc:91-103` via `dashboard_scope_payload` → `dashboard_scope_context` `DB-svc:299` | N/A | none accepted (`selected_date`/`days` not read from the request) | NO | NO | NO | NO | Adds `_system_dashboard_analytics` `DB-svc:353-358`, whose 7-statement `UNION ALL` is built with `select(...)`/`literal()`/`cast()` — **no string interpolation of user input anywhere**; the only interpolated value is the loop constant `period` (`DB-svc:674`, from the module constant `PROJECT_ACTIVITY_PERIODS`). No SQLi. |
| GET | `/api/reports/dashboard/customers/<int:customer_id>/overview` | `routes.py:88` | Global hook | `_require_dashboard_scope("dashboards.customer.view")` `routes.py:89` | `projects_query()` + `Project.customer_id == customer_id` `DB-svc:93-94` | YES — RBAC at `routes.py:89` precedes the load at `routes.py:90`; `is_active=True` filter | `<int:customer_id>` | NO | NO | NO | NO | Consistent with its HTML sibling. |
| GET | `/api/reports/dashboard/contractors/<int:contractor_id>/overview` | `routes.py:95` | Global hook | `_require_contractor_dashboard()` `routes.py:96` | `contractor_projects_query` scoping `DB-svc:379-386,410-411` | YES — `_contractor_or_404` `routes.py:97` → `contractor_is_visible` `DB-svc:389-395` (again without an `is_active` filter) | `_contractor_filters_or_400` `routes.py:98` → `DB-svc:366-376`; `project_id` re-verified `DB-svc:412-415` | NO | NO | NO | NO | `contractor_dashboard_payload` `DB-svc:526-540` does **not** include `issues`, so the cross-project issue leak above is HTML-only. It still calls the full `contractor_dashboard_context`, so all of that context's queries (including the issue query) execute and are then discarded — wasted work on every call. |

---

## A. Routes reachable without authentication

**None.** All 53 URL rules in these four blueprints are covered by the global `require_login` hook (`app/__init__.py:155-167`), whose public-endpoint allow-list is exactly `{"auth.login", "health", "healthz", "static"}` (`app/__init__.py:153`). No route in `app/project_documents/routes.py`, `app/company_media/routes.py`, or `app/dashboard/routes.py` is in that set, and no blueprint-level hook can opt out of an `@app.before_request`. Verified by reading all three router files in full plus `register_auth_guard`.

## B. Write/delete/admin routes with no proven RBAC or capability check

Every write route in scope has *some* check. The following have a check that is **weaker than the write they perform**:

1. **`GET /project-documents/projects/<project_id>`** (`routes.py:47`) — gated only by the *read* capability `can_view_documents` (`routes.py:49,51`) yet INSERTs a `ProjectDocumentFolder` + `AuditLog` and commits (`PD-svc:19-28`). Being a GET, it is also outside CSRFProtect's coverage.
2. **`POST /project-documents/folders/<id>/files/bulk-archive`** (`routes.py:214`) — route-level gate is view-only (`_bulk_folder_or_403`, `routes.py:199-203`); delete authority is proven only per file inside the service (`PD-svc:299`).
3. **`POST /project-documents/folders/<id>/files/bulk-restore`** (`routes.py:223`) — same shape (`PD-svc:318`).
4. **`POST /company-media/albums/<id>/files/bulk-archive`** (`routes.py:141`) — route gate is `p.view_album` only (`routes.py:129`); delete authority per file at `routes.py:134`; **no audit rows written at all**.
5. **`POST /company-media/albums/<id>/files/bulk-restore`** (`routes.py:143`) — same shape.
6. **`POST /company-media/albums/<id>/permissions`** (`routes.py:173`) — has a share check, but `set_permission` (`CM-svc:78-95`) constrains **neither** which flags may be granted **nor** that the actor holds them, and `_can` (`CM-perm:66-67`) makes a bare ACL row a full grant. Net effect: `can_share` ⇒ self-grant of `can_edit`/`can_delete`/`can_upload`. This is the highest-impact item in the group.
7. **`POST /company-media/albums/<id>/cover/clear`** (`routes.py:67`), **`POST /company-media/files/<id>/rename`** (`routes.py:110`), **`.../archive`** (`routes.py:118`), **`.../restore`** (`routes.py:123`) — authorization is present, but each mutates model fields inline in the route and writes **no `AuditLog`**, so these business-record mutations are unattributable. Their project_documents siblings all audit.

## C. Routes taking an ID with no owns-resource check

No route in scope loads an object by ID and uses it with **zero** verification. Four are only **partially** verified:

1. **`POST /company-media/files/<file_id>/signed-preview`** (`routes.py:99`) — album membership is verified via `file.album` (`CM-perm:78`), but `view_file` omits the `file.is_active`/`file.deleted_at` checks its own siblings perform (`CM-perm:79-81`) and that `can_view_project_document_file` performs (`PD-perm:59-60`) → archived/soft-deleted media is retrievable; and the `variant in {"preview","stream"}` branch (`CM-svc:47-52`) hands out a presigned URL to the **original** object on a view-only permission, bypassing `company_media_files.download` / ACL `can_download`.
2. **`POST /company-media/albums/<album_id>/files/complete-upload`** (`routes.py:94`) — `CM-svc:38` binds the `UploadBatchItem` on `module_type` and `target_id` but omits `target_type`, which its sibling checks (`PD-svc:136`). Currently inert because `VALID_SCOPES` (`ST-svc:14`) admits only one `target_type` per module, but it is one allow-list edit away from being live.
3. **`GET /project-documents/bulk-download-jobs/<job_id>`** (`routes.py:255`) and **`GET /company-media/bulk-download-jobs/<job_id>`** (`routes.py:168`) — `_check_job_access` (`BD-svc:280-282`) binds the job to its requester (or ADMIN/SUPER_ADMIN via literal role-string comparison, not `user.can`), but neither route verifies `job.module` matches the blueprint it was reached through, and `serialize_job` (`BD-svc:122-125`) mints a presigned ZIP URL without re-validating that the requester still has access to the files inside. Info-severity only, because `_create_job` (`BD-svc:188`) has no callers (FOUNDATION-B §6) so no reachable route can create such a job.
4. **`GET /reports/dashboard/contractors/<contractor_id>`** (`routes.py:54`) and **`GET /api/.../contractors/<contractor_id>/overview`** (`routes.py:95`) — visibility is properly enforced by `contractor_is_visible` (`DB-svc:389-395`), but `_contractor_or_404` (`routes.py:113`) applies no `is_active` filter, so archived contractors remain addressable; the sibling `customer_dashboard` does filter (`routes.py:41,90`).

## D. Routes whose checks differ from an immediate sibling in the same file

### D.1 `app/project_documents/routes.py`, read top to bottom

- `create_selection_session` (`:124`) catches only `StorageValidationError` (`:129`), yet `create_upload_selection_session` calls `_require_active_user` which raises `StorageAuthorizationError` (`ST-svc:18,200-202`) → 500. The immediate sibling `finalize_selection_session` (`:132`) **does** catch it (`:136`), and `presign_batch` (`:114`) catches it too (`:119`).
- `_bulk_folder_or_403` (`:199-203`) calls `can_view_project_document_folder` with the default `include_archived=False`, while `folder` (`:76`) passes `include_archived=True`. Bulk operations on an archived folder therefore 403 at the route rather than reaching the service's "skipped" accounting (`PD-svc:320`).
- `new_folder` (`:270`) and `move` (`:288`) take a folder ID from `request.form` rather than the URL and pass a possibly-`None` value into `db.get_or_404`. Verified against the installed SQLAlchemy 2.0.51: this returns `None` → clean 404, but emits `SAWarning: fully NULL primary key identity cannot load any object`, which under `pytest.ini`'s `filterwarnings = error` turns any test exercising the missing-parameter path into a failure. Every other route in the file uses an `<int:...>` URL converter.
- `permissions` (`:319`) computes `include_archived=not (target.is_active and target.deleted_at is None)`; the company_media sibling passes a bare `True` (`company_media/routes.py:175`). Outcomes coincide, mechanisms do not.
- `archive` (`:303`) is the only route in the file that wraps a flash message in `Markup(...)`, bypassing Jinja autoescaping. The interpolated `archived_url` comes from `url_for` with the user-supplied `q`, which Werkzeug percent-encodes, so the attribute break is not reachable — but the pattern has no sibling and no guard of its own.
- `archive` (`:296`) does not cascade to child folders or files (`PD-svc:415-418`), so a direct URL to a child of an archived parent still resolves; `restore` (`:308`) *does* require an active parent (`PD-svc:423`). The asymmetry means archived subtrees remain reachable but not restorable.

### D.2 `app/company_media/routes.py`, read top to bottom

- `presign` (`:72-78`) catches bare `Exception` and returns `str(e)` to the client; sibling `presign_batch` (`project_documents/routes.py:120`) catches only the two typed validation errors. Information disclosure.
- `complete` (`:94-97`) has no try/except and no guarded `int()`; sibling `complete_upload` (`project_documents/routes.py:145-148`) returns 400 for both. Also omits the `target_type` check its sibling makes (`PD-svc:136` vs `CM-svc:38`).
- `preview` (`:99-102`) uses `p.view_file` (no file-state check) while its immediate neighbour `download` (`:104-108`) uses `p.download_file` (file-state checked, `CM-perm:79`). Adjacent routes on the same object with different state assumptions.
- `clear_cover` (`:66-70`) writes with no audit; its immediate sibling `cover` (`:59-65`) routes through `s.set_cover`, which validates album ownership and audits (`CM-svc:74-77`).
- `file_rename` / `file_archive` / `file_restore` (`:109-126`) mutate model fields inline with no audit and (for rename) no validation; the project_documents equivalents all delegate to audited, validating service functions (`PD-svc:245-266`).
- `_bulk` (`:127-139`) neither coerces nor caps the ID list and writes no audit; sibling `bulk_archive_files`/`bulk_restore_files` (`PD-svc:292-329`) do both via `_normalize_file_ids` (`PD-svc:269-282`). `_bulk` also returns 200 when every file was forbidden, whereas `_bulk_response` (`project_documents/routes.py:206-210`) aborts 403 in that case.
- `_bulk`'s `"downloads"` branch (`:138`) calls `s.signed_download(f)` **without a user**, so `CM-svc:67`'s `user = user or db.session.get(User, f.created_by_id)` fallback attributes `ensure_bandwidth` and the `DownloadEvent` to the **file's creator instead of the caller**. This branch is currently **unreachable** — only `bulk_archive` (`"archived"`) and `bulk_restore` (`"restored"`) call `_bulk`. Info-severity as written; a latent quota/audit-attribution bypass if a third route is ever added.
- `permissions`' remove branch (`:177-180`) writes no audit; sibling `remove_folder_permission` (`PD-svc:462-465`) audits `document.folder.revoke`.

### D.3 `app/dashboard/routes.py` + `services.py`, read top to bottom

- `_require_dashboard_scope` (`:124-130`) requires `projects.scope_all`; the immediately following `_require_contractor_dashboard` (`:119-121`) does not, and `project_section_status` (`:68`) requires neither but adds a per-project `can_read_project` (`:71`). Three different authorization shapes for four sibling read endpoints — defensible, but nothing in the file documents the distinction.
- `customer_dashboard` (`:41`) and `customer_dashboard_payload` (`:90`) filter `Customer.is_active=True`; `_contractor_or_404` (`:113`) applies no `is_active` filter to `ProjectContractor`.
- `contractor_dashboard_context` gates issues on the **global** `has_any_project_capability(current_user, ("can_view_issues",))` (`DB-svc:490-491`) while `project_dashboard_context` gates the same data on the **per-project** `user_has_project_capability(current_user, project.id, "can_view_issues")` (`DB-svc:128-129`). The former is the cross-project leak in row 3 of the dashboard table.
- `project_section_status_payload` (`DB-svc:194-207`) emits `persistent_issues` counts with **no** `can_view_issues` check of any kind, while `project_dashboard_context` (`DB-svc:136`) returns `[]` without the capability.
- `DashboardScope.projects_query()` (`DB-svc:91-103`) has branches for `CUSTOMER` and `PROJECT` but **none for `CONTRACTOR`**, even though `DashboardScope.contractor()` exists (`DB-svc:87-89`) and `self.contractor_id` is declared (`DB-svc:77`). Contractor narrowing is bolted on externally in `contractor_projects_query` (`DB-svc:383-385`); any future caller that uses `DashboardScope.contractor(x).projects_query()` on its own silently receives **all** accessible projects. `DashboardScopeType.PROJECT` (`DB-svc:61`) has no constructor at all — its `projects_query` branch (`DB-svc:95-96`) is dead.

### D.4 REQUIRED PAIRED DELIVERABLE — `set_folder_permission` vs `set_permission`, side by side

`app/project_documents/services.py:429-459` vs `app/company_media/services.py:78-95`.

| Check | project_documents `set_folder_permission` | company_media `set_permission` | Verdict |
|---|---|---|---|
| `principal_type` ∈ {`user`,`role`} | YES `PD-svc:430` | YES `CM-svc:79` | parity |
| `str(principal_id).isdigit()` | YES `PD-svc:430` | YES `CM-svc:79` | parity |
| `principal_id > 0` | YES `PD-svc:433-434` | YES `CM-svc:81-82` | parity |
| target user row exists | YES `PD-svc:436-437` | YES `CM-svc:84-86` | parity |
| target user `is_active` | YES `PD-svc:437` (one combined message) | YES `CM-svc:87-88` (separate message) | parity |
| target role row exists | YES `PD-svc:439-440` | YES `CM-svc:89-90` | parity |
| target role is *grantable* (e.g. not `SUPER_ADMIN`) | **NO** | **NO** | **absent in both** — a share-capable actor can create an ACL row keyed to any `Role`, including the admin roles |
| ≥1 flag must be set | YES `PD-svc:443-444` (5 flags) | YES `CM-svc:92` (6 flags) | parity of intent |
| flag set written | `can_view, can_upload, can_edit, can_delete, can_share` `PD-svc:442,456-457` | `can_view, can_upload, can_edit, can_delete, **can_download**, can_share` `CM-svc:91,94` | **divergent** — `ProjectDocumentFolderPermission` has no `can_download` column (`models/project_document.py:79-83`); project_documents therefore **cannot express "view but not download"**, since download resolves to action `"view"` (`PD-perm:63`) |
| actor may grant only flags the actor holds | **NO** | **NO** | **absent in both**, but see the "ACL as a grant" row — only company_media is exploitable |
| upsert keyed on (container, principal) | explicit query + conditional `add` `PD-svc:446-455` | one-liner `.first() or Model(...)` `CM-svc:93` | cosmetic |
| `db.session.flush()` before audit | YES `PD-svc:458` | N/A (no audit) | — |
| **`AuditLog` on grant** | **YES** — `audit("document.folder.share", ..., new_values={"principal_type","principal_id"})` `PD-svc:458` | **NO — none** | **absent in company_media** |
| **`AuditLog` on revoke** | **YES** — `audit("document.folder.revoke", ..., old_values={"permission_id"})` `PD-svc:465` | **NO — none** (`routes.py:178-180`) | **absent in company_media** |
| revoke scoped to the parent container | YES `PD-svc:463` (`id=..., folder_id=folder.id`) | YES `routes.py:178` (`id=..., album_id=a.id`) | parity |
| revoke lives in a service function | YES `PD-svc:462-465` | **NO** — inline in the route `routes.py:177-180` | divergent |
| **ACL grant alone bypasses the module gate** | **NO** — `can_access_project_documents_module` (`auth/permissions.py:73-78`) has **no** folder-ACL clause, and `_can` AND-s the ACL with `_base` (`PD-perm:48`) | **YES** — `access()` includes `has_album_acl(user)` (`CM-perm:40`, `:18-33`) **and** `_can` short-circuits on `_matching_acl_allows` (`CM-perm:66-67`) | **divergent, and the root cause of the escalation** |
| ACL rows on an **unrestricted** container have effect | **NO** — `_acl_allows` returns `True` at `PD-perm:39-40` before reading any entry; entries are inert | **YES** — `_matching_acl_allows` is consulted first (`CM-perm:66`), before `_acl`'s `not album.is_restricted` shortcut (`CM-perm:50`) | divergent |
| ACL rows must sit on the *restriction anchor* to matter | **YES** — `_restriction_anchor` (`PD-perm:26-32`) walks to the nearest `is_restricted` **ancestor**; a grant written to a non-restricted subfolder is silently inert even though the UI accepted it | N/A — albums are flat, no ancestry | project_documents-only logic trap |

**Companion divergences in the permission-evaluation layer** (`PD-perm` vs `CM-perm`), which determine whether the grant table above is exploitable:

| Aspect | project_documents | company_media |
|---|---|---|
| `SUPER_ADMIN`/`ADMIN` | `_base` `PD-perm:11`: `is_project_admin(user)` → unconditional `True`, **no RBAC code required** | `_can` `CM-perm:60-61`: `return user.can(code)` → an `ADMIN` still needs the grant |
| `VIEWER_ADMIN` | read-only via `READ_CAPABILITIES` (`project_memberships.py:85`) / `_base` `PD-perm:12` (`can_view_documents` only) | `_can` `CM-perm:56` blocks non-`READ_ACTIONS`; `:62-63` then returns `_acl(...)`, **skipping `user.can(code)` entirely** |
| non-admin, container inside a project | `user_has_project_capability(user, folder.project_id, capability)` `PD-perm:14` — `ProjectUser` flag | N/A |
| non-admin, container with no project | RBAC pair `modules.project_documents.access` + action code `PD-perm:13` | RBAC pair `modules.company_media.access` + action code `CM-perm:68` |
| distinct file-level vs container-level permission codes | **NO** — files reuse folder codes (`can_view_documents` → `project_document_folders.view`, `PD-perm:7`) | **YES** — separate `company_media_files.*` codes `CM-perm:78-82` |
| registry codes actually consulted | **6 of 12.** Verified by grep across `app/` excluding `registry.py`: `project_document_folders.create`, `project_document_folders.delete`, `project_document_folders.restore`, `project_document_files.view`, `project_document_files.edit`, `project_document_files.download` have **zero** call sites — yet `project_document_files.download` is explicitly granted to `VIEWER_ADMIN` in `registry.py:104`. Revoking it from a role changes nothing, because download resolves to `project_document_folders.view` (`PD-perm:7,13`, `PD-perm:63`). | **12 of 12** — all `company_media_albums.*` and `company_media_files.*` codes have exactly one consumer each |
| file `is_active`/`deleted_at` checked on the **view/preview** path | YES `PD-perm:59-60` | **NO** `CM-perm:78` |
| file `is_active`/`deleted_at` checked on download/edit/delete | YES `PD-perm:62-69` | YES `CM-perm:79-81` |
| service layer re-checks the route's permission | YES on every path — `PD-svc:78, 133, 158, 182, 247, 256, 263` | **NO on any path** — `CM-svc:35, 45, 64` all trust the route |
| ACL match compared on `role_id` | `entry.role_id == user.role_id` `PD-perm:43` | `item.role_id == user.role_id` `CM-perm:46` |

**One shared latent hazard worth recording, not currently exploitable:** both ACL matchers compare `entry.role_id == user.role_id` in Python (`PD-perm:43`, `CM-perm:46`), and `has_album_acl` does the same in SQL (`CM-perm:26`). Were `user.role_id` ever `None`, `None == None` would make **every user-scoped ACL row** (whose `role_id` is `NULL` by the XOR constraint, `models/project_document.py:70`, `models/company_media.py:44`) match, and the SQL form would degrade to `role_id IS NULL`. This is not reachable today: `User.role_id` is `nullable=False` (`app/models/user.py:21`), so no persisted user can have a `NULL` role. Flagging because the safety here rests entirely on one schema attribute in a different file, with no assertion at either comparison site.

---

### Coverage note

Not examined in this pass, and therefore not asserted about: the Jinja templates for these three modules beyond the single line `app/templates/dashboard/contractor.html:48` (stored-XSS review of `display_name` / folder `name` / album `name` / `issue.title` rendering belongs to the template unit); `app/storage/{keys,validation,quota,providers}.py` (relied on via FOUNDATION-B's GUARANTEES); `tests/` — **no test file was read, so nothing here should be read as a statement about test coverage of any allow or deny path listed above.**
</content>
