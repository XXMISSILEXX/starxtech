# Copy outside the repository with mode 0600. This file names a protected
# secret file; it must not contain a database URL or any plaintext secret.
APP_DIR=/opt/starx-report
BACKUP_DIR=/opt/backups/starx-report/db
DATABASE_URL_FILE=/srv/starx-report/secrets/database_url
RETENTION_COUNT=30
