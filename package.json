{
  "private": true,
  "scripts": {
    "test": "node --test tests_js/**/*.test.js",
    "build:heic-preview": "node scripts/build-heic-preview.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.8",
    "jsdom": "^24.1.1"
  },
  "dependencies": {
    "heic-to": "1.2.0"
  }
}
