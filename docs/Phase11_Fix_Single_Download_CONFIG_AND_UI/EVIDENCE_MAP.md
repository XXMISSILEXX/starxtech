# Evidence map

Line ranges refer to the repository state verified for this phase. Runtime evidence used no real URL, object key, credential, production database, or storage provider.

| ID | Claim | Status | Confidence | File / function / line range | Test or log evidence | Notes |
|---|---|---|---|---|---|---|
| DL-001 | Company menu provides a single signed-download endpoint to JS | CONFIRMED | High | `app/templates/company_media/album.html`, file card markup, line 25 | Static inspection | Supplies `data-signed-download` and `data-download-url`. |
| DL-002 | Shared helper requires `data.ok` | CONFIRMED | Certain | `app/static/js/project-document-preview.js`, `requestJson`, line 4 | Static inspection | It throws when HTTP is non-OK **or** `data.ok` is absent/false. |
| DL-003 | Navigation happens only after helper returns | CONFIRMED | Certain | `app/static/js/project-document-preview.js`, signed-download listener, line 7 | Static inspection | `window.location.assign(result.url)` is after `await requestJson`. |
| DL-004 | Company endpoint is POST and authorization-protected | CONFIRMED | High | `app/company_media/routes.py`, `download`, lines 136-141; `app/company_media/permissions.py`, `download_file`, lines 104-108 | Existing permission tests passed | POST receives CSRF in normal Flask setup; route invokes download permission. |
| DL-005 | Company success JSON omits `ok` | CONFIRMED | Certain | `app/company_media/routes.py`, `download`, lines 136-141; `app/company_media/services.py`, `signed_download`, lines 203-212; `app/storage/providers.py`, `create_presigned_download`, lines 122-124 | Isolated SQLite/FakeStorage: `single_has_url=true`, `single_has_ok=false` | Provider payload contains `url` and `expires_at`. |
| DL-006 | Broken path does not issue signed GET | CONFIRMED | High | `app/static/js/project-document-preview.js`, lines 4 and 7 | Control-flow inspection | The throw prevents navigation. No live browser trace was needed to establish this path. |
| DL-007 | User sees generic preview error | CONFIRMED | High | `app/static/js/project-document-preview.js`, lines 4 and 7 | Static inspection | Fallback error is "Không thể tải preview." |
| DL-008 | Top-level URL field is correct | RULED OUT (mismatch) | Certain | Same files as DL-002/DL-005 | Isolated runtime | Client uses `result.url`; backend returns `url`. |
| DL-009 | Popup/anchor behavior causes fault | RULED OUT | High | `app/static/js/project-document-preview.js`, line 7 | Static inspection | No `window.open` or generated anchor in single path. |
| DL-010 | Company bulk-one uses a different valid contract | CONFIRMED | High | `app/company_media/routes.py`, `bulk_download`, lines 200-210; `app/bulk_downloads/services.py`, `request_media_download`, lines 64-69; `app/static/js/project-document-file-actions.js`, lines 87-99 | Bulk tests passed | Returns `ok:true`, `kind:direct`, and nested `download.url`. |
| DL-011 | Company bulk-many uses ZIP rather than signed single response | CONFIRMED | High | `app/bulk_downloads/services.py`, `stream_zip_download`, lines 72-112; `app/static/js/project-document-file-actions.js`, lines 102-122 | Bulk tests passed | Native form POST starts ZIP response. |
| DL-012 | Daily Report original uses GET then 302 | CONFIRMED | Certain | `app/attachments/routes.py`, `download`, lines 90-102 | Existing attachment tests | Separate navigation contract. |
| DL-013 | Project Documents menu single download shares defect | CONFIRMED | High | `app/project_documents/routes.py`, `signed_download`, lines 173-178; `app/project_documents/services.py`, `create_file_download_url`, lines 267-288; shared JS lines 4/7 | Static code-path equivalence | Backend likewise returns provider result without `ok`. |
| PRV-001 | Company preview is derivative-only | CONFIRMED | Certain | `app/company_media/services.py`, `signed_preview`, lines 179-202 | ACL/preview tests passed | Image selects thumbnail/preview; video selects poster. |
| PRV-002 | Original download does not depend on derivatives | CONFIRMED | Certain | `app/company_media/services.py`, `signed_download`, lines 203-212 | Static inspection | It signs `f.storage_object`, never queries derivative. |
| PRV-003 | HEIC decoder is intended in worker | CONFIRMED | High | `app/media_processing/pipeline.py`, lines 4-9; `requirements.txt`, lines 9-10 | Media-processing tests passed | Does not prove production container runtime health. |
| PRV-004 | Derivative failure preserves original but marks processing failed | CONFIRMED | High | `app/media_processing/pipeline.py`, `process_job`, lines 15-31; `tests/test_media_processing_foundation.py`, lines 28-36 | Test passed | Preview can be unavailable while original remains active. |
| PRV-005 | Cache/X-Accel only serves authorized derivative sources | CONFIRMED | High | `app/company_media/routes.py`, `thumbnail`, lines 122-164; `app/storage/cache.py`, lines 313-340; `deploy/nginx/starx-report.conf`, lines 9-15 | Cache tests exist/passed in prior suite; static inspection here | Original signer does not call cache. |
| PRV-006 | CSP is the primary download cause | RULED OUT | High | `app/__init__.py`, `register_security_headers`, lines 219-248 | Static inspection | Storage origin is allowed for media/connect; no signed GET is attempted first. |
| LIM-001 | Company has no dedicated limits config | CONFIRMED | High | `app/config.py`, lines 78-108; `.env.example`, lines 49-68; `docker-compose.yml`, lines 3-41 | `rg` inventory | No `COMPANY_MEDIA_` occurrence in inspected sources. |
| LIM-002 | Selection limits are 500 files / 2 GiB / 7200 seconds | CONFIRMED | High | `app/config.py`, lines 90-95; `app/storage/services.py`, lines 17-25 | Static inspection | Shared, enforced at session creation. |
| LIM-003 | Batch limits are 50 files / 512 MiB | CONFIRMED | High | `app/config.py`, lines 90-91; `app/storage/services.py`, lines 54-70 | Static inspection | JS also slices by literal 50. |
| LIM-004 | Effective image cap is 50 MiB, video cap 300 MiB | CONFIRMED | High | `app/config.py`, lines 86-95; `app/storage/validation.py`, lines 54-61 | Static inspection | Effective category cap is `min(category, absolute)`. |
| LIM-005 | Direct-S3 file body bypasses Flask/Nginx body caps | CONFIRMED | High | `app/static/js/company-media-upload.js`, direct POST implementation, lines 101-122; `app/storage/providers.py`, lines 91-114; `deploy/nginx/starx-report.conf`, line 7 | Architecture inspection | Browser posts file to provider URL, not Flask/Nginx. |
| LIM-006 | Upload/download URL TTL config is 300 seconds | CONFIRMED | High | `app/config.py`, lines 78-79 | Static inspection | Company/Project Document single signers currently use literal `300`. |
| SES-001 | Session scope binds actor/module/target/album/expiry | CONFIRMED | Certain | `app/storage/services.py`, `_selection_session`, lines 119-127; `app/models/storage.py`, lines 68-84 | Static inspection | Company passes `company_media`, `album`, album ID. |
| SES-002 | Accepted items consume presigned counters; rejected items do not | CONFIRMED | High | `app/storage/services.py`, lines 83-117 | Static inspection | Increment uses accepted objects only. |
| SES-003 | Failed HEAD item does not restore declared quota | CONFIRMED | High | `app/storage/services.py`, `complete_upload_item`, lines 130-155 | Static inspection | Failure marks item failed; no counter decrement. |
| SES-004 | Retry is non-idempotent across presign calls | CONFIRMED | Certain | `app/storage/services.py`, lines 78-117; `app/models/storage.py`, lines 99-124 | Isolated SQLite/FakeStorage output: two batches/items/pending objects; quota 1/5 → 2/10 | Same session/client ID accepted twice because uniqueness is batch-local. |
| SES-005 | Browser persists Company session ID | RULED OUT | High | `app/static/js/company-media-upload.js`, entire file; `rg sessionStorage/localStorage` | Static inspection | No persistent browser storage usage. |
| SES-006 | Company cancel endpoint exists | RULED OUT | High | `app/company_media/routes.py`, route inventory | Static inspection | No cancel route/function. |
| SES-007 | Generic pending cleanup exists; Company automatic scheduling is known | PARTIALLY CONFIRMED | Medium | `app/storage/services.py`, `cleanup_pending_uploads`, lines 170-186; `app/celery_app.py`, schedule inventory | Static inspection | Generic cleanup exists; automatic Company scheduling was not established. |
| ERR-001 | Selection count and byte failures are grouped strings | CONFIRMED | Certain | `app/storage/services.py`, lines 17-25 and 68-70 | Static inspection | Same error path/message family; no numeric details. |
| ERR-002 | Item validation can be per-file but string-only | CONFIRMED | High | `app/storage/services.py`, lines 84-112 | Static inspection | HTTP 200 includes `accepted:false,error`. |
| ERR-003 | S3 POST failure is client-side mapped | CONFIRMED | High | `app/static/js/company-media-upload.js`, `safeReason`/`directPost`, lines 20-38 and 101-122 | Static inspection | No stable backend error contract for provider POST response. |
| ERR-004 | Unexpected presign exception is sanitized | CONFIRMED | High | `app/company_media/routes.py`, lines 80-85 | Static inspection | Stable error code/message; no provider detail returned. |
| ERR-005 | Dedicated idempotency conflict contract exists | RULED OUT | High | storage/routes inventory | Isolated retry check | No detection/code exists. |
| UI-001 | UI lacks selected/max quota display | CONFIRMED | High | `app/templates/company_media/album.html`, lines 7-23 | Static inspection | No values/data attributes for limits. |
| UI-002 | Batch size 50 and concurrency 3 are hard-coded | CONFIRMED | Certain | `app/static/js/company-media-upload.js`, lines 14 and 176-183 | Static inspection | Not server-rendered config. |
| UI-003 | UI reports individual blocked/failed items | CONFIRMED | High | `app/static/js/company-media-upload.js`, `renderOverlay`, lines 37-76 | Static inspection | Partial success visible, but no structured cause/details. |
| UI-004 | Upload button disables invalid aggregate selection | RULED OUT | High | `app/static/js/company-media-upload.js`, `renderQueue`, lines 68-99 | Static inspection | It checks pending presence only. |
| SEC-001 | Client controls bucket/key for signed download | RULED OUT | High | `app/company_media/routes.py`, lines 136-141; `app/company_media/services.py`, lines 203-212 | Static inspection | Server resolves file and storage object. |
| SEC-002 | Bulk validates every selected file authorization | CONFIRMED | High | `app/bulk_downloads/services.py`, `_select_media_files`, lines 228-232; `_validate_selected`, lines 199-208 | Bulk tests passed | Validation includes active state and per-file permission. |
| SEC-003 | Company single signer validates storage active/not-deleted | RULED OUT | Certain | `app/company_media/services.py`, lines 203-212 | Service comparison | New hardening gap; Project Documents checks it at lines 271-273. |
| SEC-004 | Preview can disclose original to view-only user | RULED OUT | High | `app/company_media/services.py`, lines 185-202 | ACL preview tests passed | Preview returns derivative only. |
| SEC-005 | Signed TTL comes from config in Company single signer | RULED OUT | Certain | `app/company_media/services.py`, line 212 | Static inspection | Literal 300 is used instead of config. |
